It has functions for analyzing, cleaning, exploring, and manipulating data.
CHÚ Ý: 
df.loc[i] : truy cập vào CỘT
df[i] : truy cập vào HÀNG
import pandas as pd

#Lấy thông tin
df.info()
df.describe()
df.dtypes // Lấy thông tin từng code trong DataFrame

#SERIES: 1 cột trong table - mảng 1 chiều chứa dữ liệu
a = [1, 7, 2]
pd.Series(a) // dùng với list hoặc dict được
myvar = pd.Series(a) // Nếu không gán nhãn thì dùng chỉ số
myvar[0]

myvar = pd.Series(a, index = ["x", "y", "z"]) // Tự tạo chỉ số
myvar['x']
df = pd.DataFrame(a, columns = ["row1", "row2", "row3"]
,index = ["col1", "col2"])
//tạo tên chỉ số hàng và cột

calories = {"day1": 420, "day2": 380, "day3": 390}
myvar = pd.Series(calories, index = ["day1", "day2"]) // Dùng để chỉ định phần tử

#DATAFRAME
mảng 2D, chứa cả hàng và cột

df = pd.DataFrame(myvar)
df.loc[i] // truy cập HÀNG i lúc này là SERIES
df.loc[[i]] // truy cập HÀNG i lúc này là DATAFRAME

#Load Files Into a DataFrame
dùng với google - drive
from google.colab import drive
drive.mount('/content/drive')
file_path = '/content/drive/example.csv'

#Tạo bảng tần suất
a = pd.crosstab(bang1, bang2) // Tìm tần suất dựa trên tiêu chí của 2 bảng

#GROUPBY như trong database
car_sales.groupby(["Make"]).mean(numeric_only=True)

#Đổi kiểu
car_data["Price"] = car_data["Price"].astype(int)

1. Read .csv file
df = pd.read_csv(file_path) // Tự chuyển sang DataFrame
print(df) // Nếu set dữ liệu dài -> trả 10 dòng
print(df.to_string()) // In hết dữ liệu ra
pd.options.display.max_rows = 10000 // Tăng số cột có thể hiển thị lên

df.to_csv(file_name, sep='\t') // xuất ra .csv
2. Read JSON file
df = pd.read_json('file.json')
print(df.to_string()) 
print(df.head(i)) // in i dòng đầu tiên
print(df.tail(i)) // in i dòng cuối
print(df.info()) // in thông tin
JSON = Python Dictionary

#CLEANING DATA : 
1. Empty cells
- DELETE NAN
new_df = df.dropna() // trả df mới, không ảnh hưởng df ban đầu
df.dropna(inplace = True)
df["index"].dropna() // drop Na ở 1 CỘT cụ thể 

- FILL NAN
new_df = df.fillna(fill_value) // trả df mới, lấp đầy NaN với fill_value
df.fillna(fill_value, inplace = True)

- FILL Mean, Median, or Mode
x = df["index"].mean()  // giá trị trung bình
x = df["index"].medium() // giá trị ở giữa khi sort
x = df["index"].mod() // giá trị xuất hiện nhiều nhất
df.fillna(x, inplace = True)

2. Data in wrong format
3. Wrong data
4. Duplicates
